import { Application, Provider } from '@kifly/beagle/core/application/application';
import { appConfigurator } from '@kifly/beagle/core/application/decorators/app-configurator';

export class BasicApplication extends Application {
    public providers: Provider[] = [];

    @appConfigurator()
    public async init() {
        console.log('App is running...');
    }
}
